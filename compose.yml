services:
  peas:
    container_name: peas
    hostname: peas
    build: .
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      BLOBS_PATH: /app/blobs
      ACCESS_KEY_ID: admin
      SECRET_ACCESS_KEY: admin
    volumes:
      - peas-blobs:/app/blobs

volumes:
  peas-blobs:
